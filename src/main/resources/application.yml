server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mady-madhu/central-config.git
          searchPaths: '{application}'
          default-label: master
management:
    web:
      exposure:
        include: "*"
