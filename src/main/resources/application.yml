server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mady-madhu/central-config/tree/master/employeeService/
          default-label: master
management:
  endpoints:
    web:
      exposure:
        include: "*"
